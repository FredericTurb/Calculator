function addNumbers(one, two){return(one + two);}
function subNumbers(one, two){return(one - two);}
function mulNumbers(one, two){return(one * two);}
function divNumbers(one, two){return(one / two);}
function perNumbers(one){return(one / 100 );}


